import React from 'react';

const ProductsPage: React.FC = () => {
    return (
        <div>
            <h1>List of All Products && Product Search Page</h1>
        </div>
    );
};

export default ProductsPage;