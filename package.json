{
  "name": "ecostore",
  "version": "1.0.0",
  "description": "",
  "main": "backend/index.js",
  "scripts": {
    "start": "node backend/index.js --ignore frontend",
    "dev": "concurrently \"cd backend && npm run start\" \"cd frontend && npm run dev\"",
    "install": "cd backend && npm i && cd ../frontend && npm i",
    "seed": "cd backend && npm run seed",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
